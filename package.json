{
  "name": "web-finger-detector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "setup:git-hooks": "git config core.hooksPath scripts/git-hooks",
    "deploy": "npm run build && node scripts/deploy-dist.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.34",
    "three": "^0.184.0",
    "vite": "^8.0.10"
  }
}
