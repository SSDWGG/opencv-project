# GLB hairstyle assets

These `.glb` files are lightweight procedural hairstyle meshes generated for the browser try-on demo.

Regenerate them with:

```bash
npm run generate:hair-glb
```

The source parameters live in `src/data/hairStyles.js`, and the generator is `scripts/generate-hairstyle-glbs.mjs`.
